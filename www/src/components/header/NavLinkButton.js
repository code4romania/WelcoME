import React, {PropTypes} from 'react'

const NavLinkButton = (props, context) => {

}

NavLinkButton.propTypes = {
  // visibility function
  visible: PropTypes.func,
}

export default NavLinkButton
